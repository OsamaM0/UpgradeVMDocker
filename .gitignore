# Secrets & environment
.env
.env.*
!.env.example

# Rendered Odoo config (contains the master password)
config/odoo.conf

# Developer VM SSH keypairs generated by scripts/new-dev-vm.sh (never commit)
devs/*/keys/

# Database dumps / backups
backups/
*.sql
*.sql.gz
*.dump
*.backup
*.zip

# OS / editor cruft
.DS_Store
.idea/
*.swp
*~
